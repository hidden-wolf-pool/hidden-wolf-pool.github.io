# `nethogs` #

- **Purpose:** Groups network traffic by process.
- **Why use it:** Find applications consuming excessive bandwidth.
- **Example:**

  ```bash
  # Monitor interface eth0
  sudo nethogs eth0

  ```

- **Use case:** Detecting a malicious process sending data over the network.
