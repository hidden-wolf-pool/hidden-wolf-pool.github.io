# `multitail` #

- **Purpose:** Monitor multiple logs simultaneously in one window.

- **Why use it:** Correlate events from different sources.

- **Example:**

- ```bash
    multitail /var/log/syslog /var/log/apache2/error.log

    ```

- **Use case:** Debugging a web app by analyzing system and application logs.
